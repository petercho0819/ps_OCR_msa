import {
  IsOptional,
  IsString,
  IsNumber,
  IsNotEmpty,
  Min,
  IsIn,
  IsNumberString,
  IsArray,
} from 'class-validator';

export class UpdateReceiptDTO {
  // @IsNotEmpty()
  // @IsArray()
  // ids: Array<string>;
}
